<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylecss">
</head>

<body>

    <header class="p-3 text-bg-dark">
        <div class="container">
            <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
                    Mon blog
                </a>

                <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                    <li><a href="/newpost" class="nav-link px-2 text-secondary">Nouveau post</a></li>
                </ul>

            </div>
        </div>
    </header>
    <main>
        <div class="container">
            <h1>Bienvenue la page</h1>
            <h2>Voici les blogs</h2>
            <div class="CardsContainer">
                <% dataBlog.forEach(post_item=> { %>

                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= post_item.sujet %>
                            </h5>
                            <div>
                                <%= post_item.auteur %>
                            </div>
                            <p class="card-text">
                                <%= post_item.description %>
                            </p>
                            <div class="boutons"><a href="/editpost/<%= post_item._id %>" class="btn btn-warning">Modifier</a>

                                <form action="/deletepost?_method=DELETE" method="post">
                                    <input type="hidden" name="identifiant" value="<%= post_item._id %>">
                                    <input type="submit" value="Supprimer" class="btn btn-danger">
                                </form>

                            </div>

                        </div>
                    </div>


                    <!-- <h4><%= post_item.sujet %></h4>
            <a href="/editpost/<%= post_item._id %>"><%= post_item.auteur %></a>
            <div><%= post_item.description %></div>
            <div><%= post_item.message %></div> -->
                    <% }) %>
            </div>
        </div>
    </main>


</body>

</html>